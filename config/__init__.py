"""config — package marker so the default settings.yaml / taxonomy.json template data ships
inside the wheel (resolved at REPO_ROOT/config/... by opyt_core.config and pipeline.config).
On a fresh install these are the templates first-run bootstrap reads before writing a
user-local ~/.opyt/settings.yaml."""
